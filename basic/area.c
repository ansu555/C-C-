//Calculating Area of a Circle
#include<stdio.h>
int main()
{
    int w,l;
    printf("enter the weight : ");
    scanf("%d",&w);
    printf("enter the length :");
    scanf("%d",&l);
printf("area of a rectangle is : %d",w*l);
return 0;
}